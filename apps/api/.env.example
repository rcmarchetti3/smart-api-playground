
# Use your Render Postgres External Connection string
# Keep sslmode=require
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# Render usually injects PORT for you in hosted envs
# PORT=10000

# Optional CORS allowlist (comma-separated). If unset, allows all origins.
# ALLOWED_ORIGINS=https://your-web-app.com,https://staging-web-app.com

# Simple IP rate limiting (free tier protection)
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=60
